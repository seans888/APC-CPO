<?php
//******************************************************************
//This file was generated by Cobalt, a rapid application development 
//framework developed by JV Roig (jvroig@jvroig.com).
//
//Cobalt on the web: http://cobalt.jvroig.com
//******************************************************************
require 'path.php';
init_cobalt('Add eval hdr');

require 'components/get_listview_referrer.php';

if(xsrf_guard())
{
    init_var($_POST['btn_cancel']);
    init_var($_POST['btn_submit']);
    require 'components/query_string_standard.php';
    require 'subclasses/eval_hdr.php';
    $dbh_eval_hdr = new eval_hdr;

    $object_name = 'dbh_eval_hdr';
    require 'components/create_form_data.php';
    extract($arr_form_data);

    if($_POST['btn_cancel'])
    {
        log_action('Pressed cancel button');
        redirect("listview_eval_hdr.php?$query_string");
    }


    if($_POST['btn_submit'])
    {
        log_action('Pressed submit button');

        $message .= $dbh_eval_hdr->sanitize($arr_form_data)->lst_error;
        extract($arr_form_data);

        if($dbh_eval_hdr->check_uniqueness($arr_form_data)->is_unique)
        {
            //Good, no duplicate in database
        }
        else
        {
            $message = "Record already exists with the same primary identifiers!";
        }

        if($message=="")
        {
            $dbh_eval_hdr->add($arr_form_data);
            $id = $dbh_eval_hdr->auto_id;
            require_once 'subclasses/eval_dtl.php';
            $dbh_eval_hdr = new eval_dtl;
            for($a=0; $a<$eval_dtl_count;$a++)
            {
                
                $param = array(
                               'questionnaire_id'=>$cf_eval_dtl_questionnaire_id[$a],
                               'eval_hdr_id'=>$id,
                               'response_rating'=>$cf_eval_dtl_response_rating[$a],
                               'response_detail'=>$cf_eval_dtl_response_detail[$a]
                              );
                $dbh_eval_hdr->add($param);
            }


            redirect("listview_eval_hdr.php?$query_string");
        }
    }
}
require 'subclasses/eval_hdr_html.php';
$html = new eval_hdr_html;
$html->draw_header('Add Eval Hdr', $message, $message_type);
$html->draw_listview_referrer_info($filter_field_used, $filter_used, $page_from, $filter_sort_asc, $filter_sort_desc);

$username = "root";
$password = "";
$hostname = "localhost"; 

//connection to the database
$dbhandle = mysql_connect($hostname, $username, $password) 
 or die("Unable to connect to MySQL");
//echo "Connected to MySQL<br>";

//select a database to work with
$selected = mysql_select_db("apc-cpo",$dbhandle) 
  or die("Could not select examples");

//execute the SQL query and return records
$result = mysql_query("SELECT count(question) as count FROM questionnaire WHERE type = 'Midterm Evaluation'");
$questions = mysql_query("SELECT question, id FROM questionnaire WHERE type = 'Midterm Evaluation'");
//fetch tha data from the database 
while ($row = mysql_fetch_array($result)) {
    $i= $row['count'];
}

$n = -1;
$m = -1;

while ($row = mysql_fetch_array($questions)) {
    $z = $row['question'];
	$r = $row['id'];
	$n++;
	$m++;
	
	
//	echo '<!DOCTYPE html><meta charset="UTF-8"><html><body>';
//	echo '<input type=text name="cf_eval_dtl_question[' . $n . ']" placeholder="' . $z . '" >';
//	echo '<input type=hidden name="cf_eval_dtl_questionnaire_id[' . $m .']" value="' . $r . '">';
	
	
	$cf_eval_dtl_question[$n] = $z;
	
$cf_eval_dtl_questionnaire_id[$n] = $r;
	//echo '</body></html>';
}


//close the connection
mysql_close($dbhandle);

//echo $result;

$eval_dtl_count = $i;
$num_eval_dtl = $i;

$cf_eval_dtl_question[19] = 'The trainee possesses the needed technical skills to efficiently and effectively perform their tasks in the following specific areas: (Pls. enumerate here the task items as purported in the Intern’s Job Description.)';

$html->draw_controls('add');

$html->draw_footer();